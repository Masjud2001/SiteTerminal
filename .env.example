# ─── Database (PostgreSQL) ────────────────────────────────────
# Get these from Neon (neon.tech) — free tier works great
# In Neon dashboard: Connection string → Pooled connection = DATABASE_URL
#                    Connection string → Direct connection = DIRECT_URL
DATABASE_URL="postgresql://user:password@pooler.region.neon.tech/dbname?sslmode=require"
DIRECT_URL="postgresql://user:password@host.region.neon.tech/dbname?sslmode=require"

# ─── NextAuth ─────────────────────────────────────────────────
# Generate a secret: node -e "var c=require('crypto');console.log(c.randomBytes(32).toString('base64'));"
NEXTAUTH_SECRET="replace-with-output-of-command-above"

# In production (Vercel), set this to your deployment URL:
NEXTAUTH_URL="http://localhost:3000"

# ─── Security APIs (Admin Tools) ──────────────────────────────
# Optional: Get a key at https://account.shodan.io/
SHODAN_API_KEY="your-shodan-api-key-here"
# Optional: Get a key at https://haveibeenpwned.com/API/Key
HIBP_API_KEY="your-hibp-api-key-here"

